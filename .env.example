# TrustBridge System Environment Configuration
# Note: OpenAI API Key is optional. If empty or unavailable, Part 1 automatically
# uses the standalone rule-based heuristic NLP extraction engine.

OPENAI_API_KEY=
CONFIDENCE_THRESHOLD=0.75
DEFAULT_CURRENCY=INR
HOST=0.0.0.0
PORT=8000
